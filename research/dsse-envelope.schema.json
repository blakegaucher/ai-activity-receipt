{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AI Activity Receipt DSSE Research Envelope",
  "description": "Research-only constrained DSSE v1 envelope profile. It accepts standard and URL-safe base64 as required by the DSSE protocol, while this local profile requires keyid for trust-policy lookup.",
  "type": "object",
  "required": [
    "payloadType",
    "payload",
    "signatures"
  ],
  "properties": {
    "payloadType": {
      "type": "string",
      "format": "uri"
    },
    "payload": {
      "type": "string",
      "pattern": "^[A-Za-z0-9+/_-]*={0,2}$"
    },
    "signatures": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "keyid",
          "sig"
        ],
        "properties": {
          "keyid": {
            "type": "string",
            "minLength": 1
          },
          "sig": {
            "type": "string",
            "pattern": "^[A-Za-z0-9+/_-]+={0,2}$"
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
