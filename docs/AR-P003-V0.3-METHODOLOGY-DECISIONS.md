# AR-P003 v0.3 — Methodology Decision Ledger

> **Status:** Development-only decision control; comparison conditions, challenge design, reviewer population, primary endpoint, and primary timing clock selected; effect/precision target unresolved; protocol not frozen.
> **Snapshot:** 2026-09-23  
> **Purpose:** Preserve material differences between the current repository draft and the earlier project research review so later work cannot silently reconcile them.

AR-P003 v0.3 has reached a point where additional code can accidentally make an **unfrozen design choice look settled**. The repository therefore keeps a machine-readable methodology ledger:

- `benchmark/arp003_v0_3/methodology-decisions.schema.json`
- `benchmark/arp003_v0_3/methodology-decisions.current.json`
- `benchmark/arp003_v0_3/validate_methodology_decisions.py`

The ledger records candidate choices, their evidence source, the current implementation, and whether a deliberate preregistration decision has actually been made. The project owner selected `comparison_conditions = three_condition_structured_control` on 2026-09-20 and selected both `challenge_design = integrated_challenge_strata` and `reviewer_population = relevant_professional_reviewers` on 2026-09-21.

A separate [Methodology Decision Support](AR-P003-V0.3-DECISION-SUPPORT.md) brief and machine-readable impact map now translate every preserved candidate into its implementation/planning consequences without selecting it. The impact-map validator fails if a ledger candidate is missing or an unknown candidate is invented.

## Source-of-truth rule

For **current executable behavior**, the current repository is authoritative.

For **historical methodology recommendations**, preserve the supplied project research review:

`AI_Activity_Receipt_Research_and_v03_Protocol.pdf` / `.docx`, dated 2026-09-15.

When those sources differ, do not rewrite one to match the other. Record the conflict and require an explicit future selection before confirmatory freeze.

## Decision 1 — Comparison conditions

### Historical repository draft

The earlier executable draft used two conditions:

1. underlying heterogeneous evidence only;
2. the same underlying evidence plus Activity Receipt.

That `two_condition_current` candidate remains preserved in the machine-readable ledger and Git history. It was not silently deleted or reinterpreted.

### Earlier research-review recommendation

The research review recommended adding a **neutral structured-log / canonical event-table control**:

1. raw/underlying evidence;
2. neutral structured event table plus the same raw evidence;
3. Receipt plus the same raw evidence.

The purpose was to distinguish benefit from **organizing evidence** from any additional benefit attributable to the Receipt presentation.

### Selected status — 2026-09-20

**Selected: `three_condition_structured_control`.**

The project owner explicitly selected the three-condition design so the study can estimate separately:

- generic benefit from organizing/structuring the same evidence;
- any additional benefit attributable specifically to the Activity Receipt presentation/semantic layer.

The selection is implemented through a new development protocol/assignment/runner/scoring contract rather than mutating frozen historical evidence. The neutral structured table and Receipt must both derive material facts from the same underlying evidence available in the raw condition.

See [Comparison-Condition Decision — 2026-09-20](AR-P003-V0.3-COMPARISON-CONDITIONS-DECISION-2026-09-20.md).

This completes only the comparison-condition design decision. It does not freeze the study.

## Decision 2 — Primary confirmatory endpoint

### Current repository draft

The current protocol reports reconstruction components separately:

- material-action set precision/recall/F1;
- material-source set precision/recall/F1;
- incident set precision/recall/F1;
- exact authorization-violation accuracy;
- exact verification-state accuracy;
- exact missing-evidence accuracy;
- timing under frozen timing rules.

It explicitly avoids a post-hoc weighted composite winner score.

### Earlier research-review recommendation

The research review proposed a single interpretable primary endpoint:

**evidence-supported correct audit completion by 180 seconds**

with all required judgments correct, required evidence supporting the verdict, no fabricated material event/authorization, and timeout unable to pass. It also proposed **critical false clearance** as a safety endpoint.

### Current status

**Selected: `correct_completion_by_180s`.**

The selected binary primary endpoint is evidence-supported correct audit completion by 180 seconds. All prespecified required judgments must be correct and supported under the frozen case-specific endpoint contract; added material facts or mismatched authorization state fail the content rule; and a deadline miss cannot succeed.

The existing component outcomes remain secondary diagnostics rather than a co-primary set. Critical false clearance remains a separate safety endpoint with its threshold still unresolved under `effect_precision_target`.

The primary endpoint decision did not itself select the clock. That separate decision was made on 2026-09-23: `primary_timing_clock = wall_deadline_with_hidden_sensitivity`. Development still records both clocks; wall time is primary by methodology and active time remains secondary/sensitivity. See [Primary Endpoint Decision — 2026-09-21](AR-P003-V0.3-PRIMARY-ENDPOINT-DECISION-2026-09-21.md) and [Primary Timing Clock Decision — 2026-09-23](AR-P003-V0.3-PRIMARY-TIMING-CLOCK-DECISION-2026-09-23.md).

## Decision 3 — Primary timing clock

### Current development runner

The runner records both:

- wall elapsed time;
- active elapsed time.

Manual pause and page visibility can pause the active clock. Analysis requires an explicit active-vs-wall timing choice.

### Earlier research-review recommendation

The research review recommended that elapsed background time remain in the primary deadline and that trials with more than 10 seconds hidden be handled through a prespecified sensitivity analysis rather than silently pausing selected trials.

### Selected status — 2026-09-23

**Selected: `wall_deadline_with_hidden_sensitivity`.**

The primary 180-second deadline uses wall elapsed time. Background/hidden elapsed time remains inside that deadline. Active time remains a secondary/sensitivity measure. Trials with more than 10 seconds hidden are reserved for a prespecified sensitivity analysis and are not converted into active-time primary successes.

The exact technical-failure, reload/context-break, timeout/last-answer, and allowed browser/device rules remain open freeze requirements. This selection does not authorize recruitment or execution.

See [Primary Timing Clock Decision — 2026-09-23](AR-P003-V0.3-PRIMARY-TIMING-CLOCK-DECISION-2026-09-23.md).

## Decision 4 — Misleading-Receipt robustness design

### Current repository draft

The current protocol integrates prespecified challenge strata:

- stale Receipt;
- incomplete Receipt;
- conflicting Receipt.

Ordinary and challenge results are reported separately.

### Earlier research-review recommendation

The research review proposed a **separate robustness cohort** comparing raw-only, faithful Receipt + raw, and corrupted Receipt + raw presentations using fresh families/episodes.

### Selected status — 2026-09-21

**Selected: `integrated_challenge_strata`.**

The project owner selected the integrated architecture so stale, incomplete, and conflicting Receipt cases remain inside the same crossed study as ordinary cases, while ordinary and challenge results are reported separately.

The rejected `separate_robustness_cohort` recommendation remains preserved as historical methodology evidence. Final challenge counts/allocation, sealed-case review, and challenge-specific balance remain open freeze requirements.

See [Challenge-Design Decision — 2026-09-21](AR-P003-V0.3-CHALLENGE-DESIGN-DECISION-2026-09-21.md).

## Decision 5 — Target reviewer population

### Preserved prior state

The population was previously unresolved and represented by the `population_to_be_selected` placeholder. That state remains preserved in Git history and the earlier dated continuity snapshot.

### Selected status — 2026-09-21

**Selected: `relevant_professional_reviewers`.**

Eligibility requires at least 1 year of relevant professional/practical experience in one or more listed audit, cybersecurity, compliance, AI-governance, incident-review, software/system-operations, technical-assurance, or closely related evidence-review domains. Record one experience band: 1–2, 3–5, or 6+ years.

Sufficient English proficiency is required. No particular degree, certification, or job title is required. Prior general AI Activity Receipt familiarity is permitted and recorded.

Case constructors/material editors, people who created or accessed hidden gold, scoring-rule developers who used sealed cases, confirmatory-response scorers, and people with premature protected-analysis access are excluded. During study cases, external web search, external AI assistants, another person, and outside tools/evidence are prohibited absent an explicitly versioned pre-execution protocol amendment.

Claims remain scoped to the professional population actually recruited. This selection does not authorize recruitment or set final sample size, domain quotas, allocation, or stopping.

See [Reviewer-Population Decision — 2026-09-21](AR-P003-V0.3-REVIEWER-POPULATION-DECISION-2026-09-21.md).

## Decision 6 — Meaningful effect / precision target

The repository now contains:

- independent-observation screening calculations;
- design-specific reviewer × case Monte Carlo sensitivity tooling.

Those tools deliberately use illustrative assumptions.

v0.2.3 cannot supply a defensible human effect estimate because the auxiliary AI reviewer was at ceiling in both conditions and no human timing data exist.

**Status: unresolved.**

A minimally meaningful effect or precision target must be chosen from a defensible external, development-only, or decision-theoretic basis before final power/sample-size freeze.

## Machine guard

Run:

```bash
python benchmark/arp003_v0_3/validate_methodology_decisions.py
python benchmark/arp003_v0_3/validate_methodology_decisions.py --self-test
```

The validator requires:

- ledger/protocol version agreement;
- all required decision IDs to remain present;
- unique decision and candidate IDs;
- a selected candidate to resolve to a listed candidate;
- selected decisions to have rationale and evidence references;
- unresolved decisions to carry no selected candidate;
- every pre-freeze methodology decision to be selected before the ledger may become `methodology_resolved`;
- no frozen protocol while required methodology decisions remain unresolved.

For the selected reviewer population, it also fail-closes on drift in the eligible domains, minimum experience, experience bands, English rule, degree/certification/title rule, project-familiarity recording, exclusions, external-assistance rule, inference boundary, or recruitment authorization.

The six mandatory decision IDs must retain `pre_freeze_required=true`; flipping
that flag cannot remove an unresolved decision from the freeze guard. The
freeze-readiness checker reuses this complete validator for its supplied ledger,
including custom `--methodology` inputs, rather than trusting `selected` labels
alone. See [freeze-readiness checks](AR-P003-V0.3-FREEZE-READINESS.md#methodology-cross-check).

## Freeze-readiness integration

The freeze-readiness model now includes an explicit **comparison-conditions** gate.

The comparison-conditions, reviewer-population, primary-endpoint, and primary-timing methodology decisions are selected and explicit in controlled artifacts. The timing/exclusions freeze-readiness gate remains prepared because implementation-specific technical rules and browser/device smoke evidence are still open. The effect target remains unresolved. The challenge-design decision is selected, but the separate challenge-strata freeze-readiness gate remains prepared because final counts/allocation and final-corpus review are not complete.

The preserved historical conflict remains part of the evidence trail rather than being rewritten to match the selected design.

## What this does not do

This ledger does not:

- reopen the selected wall-clock timing decision;
- select the effect/precision target;
- choose the meaningful effect size;
- freeze sample size;
- obtain ethics review;
- authorize or conduct reviewer recruitment;
- create human evidence.

Those remain deliberate preregistration, external-review, or execution gates.

## Change-control rule

When a methodology decision is made:

1. update only that decision from `unresolved` to `selected`;
2. name the selected candidate;
3. record the rationale and evidence references;
4. update the relevant protocol/runner/scorer/assignment artifacts in a **new version if the choice changes methodology**;
5. update freeze-readiness evidence;
6. rerun CI and the reproducibility suite;
7. preserve the rejected alternative and the earlier source recommendation in this ledger;
8. do not reinterpret the frozen v0.2.3 benchmark.

A selected methodology is not evidence that the selected method produces a favorable result.
