# AR-P003 v0.3 — Effect / Precision Target Decision Proposal

> **Status:** Proposal for owner review; not selected on `main`.  
> **Date:** 2026-09-23  
> **Base:** `main` after merge of PR #95.  
> **Scope:** Resolve only `effect_precision_target` if approved. Do not reopen previously selected methodology. Do not authorize recruitment, human execution, or confirmatory freeze.

## Current controlled state

Selected and preserved:

- `comparison_conditions = three_condition_structured_control`
- `challenge_design = integrated_challenge_strata`
- `reviewer_population = relevant_professional_reviewers`
- `primary_endpoint = correct_completion_by_180s`
- `primary_timing_clock = wall_deadline_with_hidden_sensitivity`

Still unresolved:

- `effect_precision_target`

AR-P003 v0.3 remains development-only, not frozen, not executed, and recruitment remains unauthorized.

## Proposed decision

Select:

`effect_precision_target = research_review_proposed_thresholds`

with the following project-specific preregistration decision assumptions:

1. **+5 percentage points** absolute supported-completion improvement as the smallest practically important Receipt-specific benefit.
2. **3 percentage points** as the maximum accuracy loss permitted by a separate noninferiority analysis if a speed claim is ever pursued.
3. **+2 percentage points** as the maximum acceptable absolute increase in critical false clearance.

These values are owner/project decision thresholds. They are **not industry norms**, not estimates from AR-P003 v0.2.3, and not evidence that the true effects equal these values.

## Contrast mapping for the three-condition design

The selected three-condition architecture exists to distinguish generic organization benefit from Receipt-specific incremental benefit.

For effect-target planning:

- `receipt_vs_structured` is the direct Receipt-specific incremental contrast and is the proposed contrast to which the +5 percentage-point minimum practically important benefit applies.
- `receipt_vs_raw` remains an important total-presentation contrast and should be reported as prespecified supporting evidence.
- `structured_vs_raw` remains the generic-structuring/context contrast and should be reported separately.

This mapping does not convert either supporting contrast into a hidden post-hoc primary claim.

## Critical-false-clearance guardrail

Critical false clearance remains a separate safety endpoint, not part of the binary primary endpoint.

The proposed +2 percentage-point guardrail should be checked for Receipt relative to both relevant comparators:

- Receipt vs structured; and
- Receipt vs raw.

A favorable primary supported-completion result should not be presented as an unqualified benefit if the prespecified safety analysis shows a Receipt-associated critical-false-clearance increase above the accepted guardrail.

The exact inferential procedure and uncertainty rule for the safety endpoint still must be frozen in the final analysis plan.

## Separate speed-claim margin

The proposed 3 percentage-point noninferiority margin is **dormant unless a separate speed claim is preregistered**.

A speed claim would require, before execution:

- a separately defined accuracy-preservation endpoint;
- the 3 percentage-point noninferiority margin applied to that endpoint;
- a prespecified inferential rule;
- reliable timing under the selected wall-clock design.

Selecting this candidate does not itself authorize or require a speed claim.

## Why this is the next defensible decision

The repository cannot complete crossed-design power/precision planning while the minimum effect or precision target remains unspecified.

The earlier v0.2.3 auxiliary evidence cannot estimate a human effect because it used an auxiliary AI reviewer, reached ceiling non-timing results, and had no usable human timing distribution. Using the observed auxiliary result to choose a favorable human threshold would therefore be inappropriate.

The preserved research-review thresholds are instead explicit, pre-outcome decision assumptions. Selecting them now would let the crossed-design planner evaluate feasibility without inspecting confirmatory outcomes.

## External methodological framing

External sources support the **methodological structure**, not the proposed numeric values.

NIST usability guidance treats task success/completion and time on task as standard quantitative measures of effectiveness and efficiency, and its usability-requirements work emphasizes specifying performance criteria for the intended users, tasks, and context of use. That supports using an application-specific practical threshold rather than presenting +5pp as a universal usability norm.

FDA noninferiority guidance is clinical-trial guidance and is **not directly applicable to AR-P003**, but it illustrates a useful general design principle: a noninferiority margin should be justified and fixed prospectively rather than selected after results are known. AR-P003 adopts only that general prespecification principle for the conditional 3pp speed-claim margin; the FDA guidance does not validate 3pp for this project.

References:

- NIST, Usability Testing: https://www.nist.gov/programs-projects/usability-testing
- NIST, Common Industry Specification for Usability — Requirements: https://www.nist.gov/publications/common-industry-specification-usability-requirements
- FDA, Non-Inferiority Clinical Trials (2016): https://www.fda.gov/regulatory-information/search-fda-guidance-documents/non-inferiority-clinical-trials

No external source identified here establishes +5pp, 3pp, or +2pp as an industry standard for AI audit tools. The proposed numbers remain project-specific owner decision assumptions.

## Required planning after approval

If this proposal is approved and implemented, the next development-only planning pass should:

- evaluate reviewer counts 108 / 216 / 324 / 432 or document a replacement grid;
- use at least 1,000 Monte Carlo replicates per decision-relevant scenario;
- include plausible baseline supported-completion sensitivity;
- include reviewer- and case-heterogeneity sensitivity;
- evaluate the +5pp `receipt_vs_structured` target explicitly;
- keep the +2pp critical-false-clearance guardrail separate from the primary endpoint;
- preserve `receipt_vs_raw` and `structured_vs_raw` reporting;
- freeze neither sample size nor allocation until the resulting feasibility analysis is reviewed.

## Non-authorization statement

This proposal does **not**:

- freeze AR-P003 v0.3;
- authorize reviewer recruitment;
- authorize human-study execution;
- freeze reviewer count, case count, allocation, or stopping;
- claim that Activity Receipts improve accuracy, speed, safety, compliance, or productivity;
- establish these thresholds as external standards or norms.

## Merge meaning

This proposal PR alone is preparatory. A later implementation PR should update the machine-readable methodology ledger only after explicit owner approval of the threshold bundle and contrast mapping.
